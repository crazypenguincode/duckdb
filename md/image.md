```mermaid
graph LR
%% =============== 子图1: 布隆过滤器核心原理 ===============
    subgraph "布隆过滤器核心原理"
        direction TB
        A[元素集合] --> B[k个哈希函数]
        B --> C[位数组 m bits]
        C --> D[插入操作: 设置k个位为1]
        C --> E[查询操作: 检查k个位是否都为1]

        D --> F[一定不存在: 返回False]
        E --> G[可能存在: 返回True]
        E --> F
    end

%% =============== 子图2: 变种及优点 ===============
    subgraph "布隆过滤器变种及优点"
        direction TB
        K[标准布隆过滤器]
        L[计数布隆过滤器]
        M[分层布隆过滤器]
        N[动态布隆过滤器]

        K --> K1[" 空间效率极高<br/>查询速度快<br/>实现简单"]
        L --> L1[" 支持删除操作<br/>精确计数<br/>内存开销增加"]
        M --> M1[" 降低假阳性率<br/>总假阳性率=p^L<br/>复杂度增加"]
        N --> N1[" 支持动态扩容<br/>适应数据量变化<br/>扩容开销"]
    end

%% =============== 子图3: 在数据库中的应用优势 ===============
    subgraph "在数据库中的应用优势"
        direction TB
        O[LSM-Tree存储引擎]
        P[分布式数据库]
        Q[查询缓存优化]
        R[连接操作优化]

        O --> O1[" 避免不必要磁盘I/O<br/>提升读性能"]
        P --> P1[" 减少网络通信<br/>快速定位数据节点"]
        Q --> Q1[" 快速预筛选<br/>减少缓存查找开销"]
        R --> R1[" 过滤不匹配记录<br/>优化大表连接"]
    end

%% =============== 子图4: 性能特征对比 ===============
    subgraph "性能特征对比"
        direction TB
        S["时间复杂度: O(k)"]
        T["空间复杂度: O(m)"]
        U[假阳性率: 可控制]
        V[假阴性率: 0]
    end

%% =============== 样式定义 ===============
    style A fill:#e3f2fd
    style C fill:#fff3e0
    style K1 fill:#e8f5e8
    style L1 fill:#fff8e1
    style M1 fill:#f3e5f5
    style N1 fill:#e0f2f1
```

```mermaid
graph TD
%% =============== 子图1: 布隆过滤器核心原理 ===============

        A[元素集合] --> B[k个哈希函数]
        B --> C[位数组 m bits]
        C --> D[插入操作: 设置k个位为1]
        C --> E[查询操作: 检查k个位是否都为1]

        D --> F[一定不存在: 返回False]
        E --> G[可能存在: 返回True]
        E --> F

```
    

```mermaid
graph LR


%% =============== 子图2: 变种及优点 ===============
    subgraph "布隆过滤器变种及优点"
        direction TB
        K[标准布隆过滤器]
        L[计数布隆过滤器]
        M[分层布隆过滤器]
        N[动态布隆过滤器]

        K --> K1[" 空间效率极高<br/>查询速度快<br/>实现简单"]
        L --> L1[" 支持删除操作<br/>精确计数<br/>内存开销增加"]
        M --> M1[" 降低假阳性率<br/>总假阳性率=p^L<br/>复杂度增加"]
        N --> N1[" 支持动态扩容<br/>适应数据量变化<br/>扩容开销"]
    end

%% =============== 子图3: 在数据库中的应用优势 ===============
    subgraph "在数据库中的应用优势"
        direction TB
        O[LSM-Tree存储引擎]
        P[分布式数据库]
        Q[查询缓存优化]
        R[连接操作优化]

        O --> O1[" 避免不必要磁盘I/O<br/>提升读性能"]
        P --> P1[" 减少网络通信<br/>快速定位数据节点"]
        Q --> Q1[" 快速预筛选<br/>减少缓存查找开销"]
        R --> R1[" 过滤不匹配记录<br/>优化大表连接"]
    end


```

| 查询类型         | 10次测试 | 50次测试 | 100次测试 | 200次测试 |
|------------------|----------|----------|-----------|-----------|
| 简单聚合查询     | 0.33     | 0.30     | 0.29      | 0.30      |
| 多表连接查询     | 1.33     | 1.33     | 1.33      | 1.87      |
| 复杂分析查询     | 6.11     | 5.81     | 5.03      | 5.10      |
| 窗口函数查询     | 1.33     | 1.92     | 1.45      | 1.44      |

| 测试次数 | 无缓存 (s) | 有缓存 (s) |
|----------|------------|------------|
| 10次     | 0.203      | 0.086      |
| 50次     | 0.795      | 0.389      |
| 100次    | 1.646      | 0.524      |
| 200次    | 2.998      | 1.186      |